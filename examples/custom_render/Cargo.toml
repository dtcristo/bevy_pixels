[package]
name = "example_custom_render"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bevy = { version = "0.14", default-features = false }
bevy_pixels = { path = "../../", default-features = false }
