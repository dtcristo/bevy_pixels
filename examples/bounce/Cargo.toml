[package]
name = "example_bounce"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
bevy = { version = "0.13", default_features = false }
bevy_pixels = { path = "../../" }
rand = "0.8"
